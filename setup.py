from setuptools import setup
from setuptools.extension import Extension

setup(name='multiscaleRD',
      version='0.1',
      description='Coupling particle-based simulations with reaction-diffusion PDEs',
      author='',
      author_email='',
      url='',
      packages=['multiscaleRD'],
      #cmdclass = cmdclass,
      )
